name: Deploy Frontend to S3 & CloudFront

on:
  push:
    branches:
      - prod-main  # or your deployment branch
    paths:
      - 'client/**'  # only trigger when frontend changes

jobs:
  deploy:
    name: Deploy React Frontend
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: client  # operate inside the client folder

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Create .env file
        run: |
          echo "VITE_API_BASE_URL_DEVELOPMENT=${{ secrets.VITE_API_BASE_URL_DEVELOPMENT }}" >> .env
          echo "VITE_API_BASE_URL_STAGING=${{ secrets.VITE_API_BASE_URL_STAGING }}" >> .env
          echo "VITE_API_BASE_URL_PRODUCTION=${{ secrets.VITE_API_BASE_URL_PRODUCTION }}" >> .env
          echo "VITE_CLOUDINARY_CLOUD_NAME=${{ secrets.VITE_CLOUDINARY_CLOUD_NAME }}" >> .env
          echo "VITE_CLOUDINARY_UPLOAD_PRESET=${{ secrets.VITE_CLOUDINARY_UPLOAD_PRESET }}" >> .env
          echo "VITE_CLOUDINARY_URL=${{ secrets.VITE_CLOUDINARY_URL }}" >> .env
          echo "VITE_CLOUDINARY_SCREENSHOT_UPLOAD_PRESET=${{ secrets.VITE_CLOUDINARY_SCREENSHOT_UPLOAD_PRESET }}" >> .env
          echo "VITE_CLOUDINARY_URL_SS=${{ secrets.VITE_CLOUDINARY_URL_SS }}" >> .env
          echo "VITE_CONTACT_FORM_SCRIPT_URL=${{ secrets.VITE_CONTACT_FORM_SCRIPT_URL }}" >> .env

      - name: Build the React app
        run: npm run build:staging

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Sync build to S3 /frontend folder (excluding index.html)
        run: |
          aws s3 sync dist/ s3://$S3_BUCKET_NAME/frontend \
            --delete \
            --exclude "index.html" \
            --cache-control "max-age=31536000,public"
        env:
          S3_BUCKET_NAME: ${{ secrets.S3_BUCKET_NAME }}

      - name: Upload index.html with no-cache
        run: |
          aws s3 cp dist/index.html s3://$S3_BUCKET_NAME/frontend/index.html \
            --cache-control "no-cache, no-store, must-revalidate" \
            --content-type "text/html"
        env:
          S3_BUCKET_NAME: ${{ secrets.S3_BUCKET_NAME }}

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id $CLOUDFRONT_DISTRIBUTION_ID \
            --paths "/*"
        env:
          CLOUDFRONT_DISTRIBUTION_ID: ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}

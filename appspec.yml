version: 0.0
os: linux
files:
  - source: server
    destination: /home/ubuntu

hooks:
  AfterInstall:
    - location: scripts/install.sh
      runas: ubuntu

  ApplicationStart:
    - location: scripts/start.sh
      runas: ubuntu
